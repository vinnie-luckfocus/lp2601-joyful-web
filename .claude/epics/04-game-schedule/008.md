---
name: Refactor GameCard and build ScheduleGameCard
status: open
created: 2026-04-09T09:41:06Z
updated: 2026-04-09T09:41:06Z
github: [Will be updated when synced to GitHub]
depends_on: [006, 007]
parallel: false
conflicts_with: [006, 007]
---

# Task: Refactor GameCard and build ScheduleGameCard

## Description

Refactor the existing `GameCard` into smaller shared sub-components, then build a `ScheduleGameCard` that composes them with attendance-specific features for the schedule page.

## Acceptance Criteria

- [ ] `GameCard` is split into reusable sub-components (e.g., `GameCardTeams`, `GameCardMeta`, `GameCardFooter`) without breaking the homepage `RecentGames` usage
- [ ] `ScheduleGameCard` composes these sub-components and adds:
  - Signup status badge (e.g., "已报名")
  - Attendee count display
  - 4px MLB Red (`#BF0D3E`) left border when the current user's status is `confirmed`
- [ ] Homepage `GameCard` remains visually unchanged (no regression)
- [ ] `ScheduleGameCard` integrates cleanly into the timeline layout on `GameSchedulePage`

## Technical Details

- Keep shared presentation logic DRY
- Avoid prop bloat by having `ScheduleGameCard` pass attendance data only to its own layer
- Update `frontend/src/components/games/GameCard.tsx` and related files

## Dependencies

- [ ] 006 — GameSchedulePage structure must be in place
- [ ] 007 — attendance data contracts must be stable

## Effort Estimate

- Size: M
- Hours: 5
- Parallel: false

## Definition of Done

- [ ] Code implemented
- [ ] No visual regressions on homepage GameCard
- [ ] ScheduleGameCard renders correctly in timeline
