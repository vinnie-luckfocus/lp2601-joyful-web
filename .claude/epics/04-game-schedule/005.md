---
name: Register new backend routes in app router
status: open
created: 2026-04-09T09:41:06Z
updated: 2026-04-09T09:41:06Z
github: [Will be updated when synced to GitHub]
depends_on: [003, 004]
parallel: true
conflicts_with: []
---

# Task: Register new backend routes in app router

## Description

Wire the new `games` and `attendance` routes into the Express application so all endpoints are reachable.

## Acceptance Criteria

- [ ] `backend/src/app.ts` (or equivalent router index) mounts `/api/games` from `backend/src/routes/games.ts`
- [ ] `/api/games/:id/attend` is reachable via the mounted route file (can be in `games.ts` or a dedicated `attendance.ts`)
- [ ] Smoke tests verify all 5 new endpoints respond with expected HTTP status codes
- [ ] No 404 regressions on existing routes

## Technical Details

- Follow existing route registration patterns in the backend
- Keep route organization consistent with current file structure

## Dependencies

- [ ] 003 — protected game routes must be implemented
- [ ] 004 — attendance routes must be implemented

## Effort Estimate

- Size: XS
- Hours: 1
- Parallel: true

## Definition of Done

- [ ] Code implemented
- [ ] Smoke tests passing
- [ ] Verified via local curl/HTTP client
