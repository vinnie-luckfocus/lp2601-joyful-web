---
name: Implement protected game list and detail endpoints
status: open
created: 2026-04-09T09:41:06Z
updated: 2026-04-09T09:41:06Z
github: [Will be updated when synced to GitHub]
depends_on: [001]
parallel: true
conflicts_with: []
---

# Task: Implement protected game list and detail endpoints

## Description

Create `backend/src/routes/games.ts` with authenticated endpoints for the full game list and game detail, including the current user's attendance status per game.

## Acceptance Criteria

- [ ] `GET /api/games` requires JWT auth and returns all games with `my_status` per game (`confirmed` | `declined` | `null`)
- [ ] `GET /api/games/:id` requires JWT auth and returns full match details plus `my_status`
- [ ] Responses use standard `{ success, data, error, meta }` envelope
- [ ] SQL queries are parameterized and join `games`, `teams`, and `game_attendance` efficiently
- [ ] Integration tests cover authenticated access, data shape, and `my_status` accuracy

## Technical Details

- Reuse existing `authMiddleware` from `backend/src/middleware/auth.ts`
- Create `backend/src/routes/games.ts` and corresponding service logic
- `my_status` is derived by joining `game_attendance` on the authenticated user's ID

## Dependencies

- [ ] 001 — games and game_attendance tables must exist

## Effort Estimate

- Size: S
- Hours: 4
- Parallel: true

## Definition of Done

- [ ] Code implemented
- [ ] Tests written and passing
- [ ] Verified with authenticated requests locally
