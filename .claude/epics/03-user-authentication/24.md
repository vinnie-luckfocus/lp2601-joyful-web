---
name: Fix JWT expiry to 7 days
status: open
created: 2026-04-09T05:28:31Z
updated: 2026-04-09T05:48:12Z
github: https://github.com/vinnie-luckfocus/lp2601-joyful-web/issues/24
depends_on: [23]
parallel: true
conflicts_with: [25, 26]
---

# Task: Fix JWT expiry to 7 days

## Description
Update the JWT token expiry from `2h` to `7d` in the auth middleware to align with the PRD and epic architecture decision.

## Acceptance Criteria
- [ ] `backend/src/middleware/auth.ts` uses `expiresIn: '7d'` instead of `'2h'`
- [ ] All existing protected routes continue to validate tokens correctly
- [ ] Backend tests are updated to expect the new expiry or remain compatible
- [ ] `backend/src/app.ts` adds `helmet` middleware for security headers (CSP, X-Frame-Options, etc.)
- [ ] CORS configuration in `backend/src/app.ts` rejects wildcard `*` in `ALLOWED_ORIGINS`
- [ ] `backend/.env` JWT secret is replaced with a placeholder or documented rotation requirement (no hardcoded production secret committed)

## Technical Details
- Files: `backend/src/middleware/auth.ts`, `backend/src/app.ts`, `backend/.env`
- Change `expiresIn: '2h'` → `expiresIn: '7d'`
- Verify no other hardcoded expiry values exist in the auth module
- Install and configure `helmet` with reasonable defaults
- Validate `ALLOWED_ORIGINS` explicitly rejects `*`
- Update `.env` example or add README note about generating a strong `JWT_SECRET`

## Dependencies
- [ ] Task 001 (database foundation)

## Effort Estimate
- Size: XS
- Hours: 1
- Parallel: true

## Definition of Done
- [ ] Code implemented
- [ ] Tests written and passing
- [ ] Documentation updated
- [ ] Code reviewed
- [ ] Deployed to staging
