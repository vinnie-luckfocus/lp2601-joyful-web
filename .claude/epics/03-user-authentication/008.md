---
name: Wire Navbar to auth store on HomePage
status: open
created: 2026-04-09T05:28:31Z
updated: 2026-04-09T05:28:31Z
github: [Will be updated when synced to GitHub]
depends_on: [005]
parallel: true
conflicts_with: []
---

# Task: Wire Navbar to auth store on HomePage

## Description
Connect the `Navbar` component's auth props (`isLoggedIn`, `userName`, `onLoginClick`) to `useAuthStore` in the `HomePage` so the navbar correctly reflects authentication state and can trigger the login modal.

## Acceptance Criteria
- [ ] `frontend/src/pages/Home/index.tsx` reads `isAuthenticated` and `user?.name` from `useAuthStore`
- [ ] `Navbar` receives `isLoggedIn` and `userName` props dynamically
- [ ] Clicking "登录" in the navbar opens the login modal (or navigates to `/login` as fallback)
- [ ] Clicking logout in the navbar triggers the store's `logout` action
- [ ] Navbar updates immediately after login/logout without a page refresh

## Technical Details
- Files: `frontend/src/pages/Home/index.tsx`, `frontend/src/components/Navbar/index.tsx`
- Ensure the `Navbar` prop interface supports the required callbacks
- Consider extracting a small `HomePageHeader` wrapper if prop drilling becomes complex

## Dependencies
- [ ] Task 005 (auth store state is stable)

## Effort Estimate
- Size: XS
- Hours: 1
- Parallel: true

## Definition of Done
- [ ] Code implemented
- [ ] Tests written and passing
- [ ] Documentation updated
- [ ] Code reviewed
- [ ] Deployed to staging
