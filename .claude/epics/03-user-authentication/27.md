---
name: Extend auth store with changePassword and is_first_login
status: open
created: 2026-04-09T05:28:31Z
updated: 2026-04-09T05:48:12Z
github: https://github.com/vinnie-luckfocus/lp2601-joyful-web/issues/27
depends_on: [25, 26]
parallel: true
conflicts_with: []
---

# Task: Extend auth store with changePassword and is_first_login

## Description
Update the frontend Zustand auth store (`useAuthStore`) to handle `is_first_login` from the login response and provide a `changePassword` action that calls the new backend endpoint.

## Acceptance Criteria
- [ ] `frontend/src/stores/auth.ts` stores `is_first_login` on the user object after login
- [ ] New `changePassword(old_password, new_password)` action calls `POST /api/auth/change-password` with PRD-aligned field names
- [ ] On successful password change, store clears token from `localStorage`, resets auth state, and requires the user to re-login (per PRD Story 2 acceptance criteria)
- [ ] Store removes any `console.log` statements in production code paths
- [ ] Types for `User` and auth state are updated accordingly

## Technical Details
- File: `frontend/src/stores/auth.ts`
- Use the existing axios instance for API calls
- Handle 401/unauthorized errors gracefully (dispatch logout or show error)
- After successful password change: clear `localStorage` token, set `user` to `null`, `isAuthenticated` to `false`, and redirect to login modal/page so the user must re-login

## Dependencies
- [ ] Task 003 (login response includes `is_first_login`)
- [ ] Task 004 (`change-password` endpoint exists)

## Effort Estimate
- Size: S
- Hours: 2
- Parallel: true

## Definition of Done
- [ ] Code implemented
- [ ] Tests written and passing
- [ ] Documentation updated
- [ ] Code reviewed
- [ ] Deployed to staging
