---
name: Backend auth integration test coverage
status: open
created: 2026-04-09T05:28:31Z
updated: 2026-04-09T05:37:33Z
github: [Will be updated when synced to GitHub]
depends_on: [002, 003, 004]
parallel: false
conflicts_with: []
---

# Task: Backend auth integration test coverage

## Description
Expand backend auth test coverage to include the `/me` happy path and the new `/change-password` endpoint. Fix any test quality issues identified in the audit (mutable fixtures, mocked database concerns).

## Acceptance Criteria
- [ ] Add happy-path test for `GET /api/auth/me` with a valid JWT
- [ ] Add tests for `POST /api/auth/change-password` (success, wrong old password, weak new password < 6 chars, unauthenticated)
- [ ] Fix mutable test fixture in `auth.test.ts` (`mockUser.password_hash` mutation in `beforeAll`)
- [ ] Remove `console.log` statements from `backend/src/routes/auth.ts` production code paths
- [ ] Remove or replace mocked database concerns where practical, or document why mocks are sufficient
- [ ] Add a performance assertion ensuring `POST /api/auth/login` responds in < 500ms under normal load
- [ ] All backend auth tests pass with `npm test` in the backend directory

## Technical Details
- File: `backend/src/__tests__/routes/auth.test.ts`
- Use supertest for HTTP assertions
- Generate a valid JWT in tests using the test secret
- Ensure password hashes are computed immutably (spread or pre-compute)
- Keep tests focused and fast

## Dependencies
- [ ] Task 002 (JWT expiry fixed)
- [ ] Task 003 (login response updated)
- [ ] Task 004 (change-password endpoint implemented)

## Effort Estimate
- Size: M
- Hours: 4
- Parallel: false

## Definition of Done
- [ ] Code implemented
- [ ] Tests written and passing
- [ ] Documentation updated
- [ ] Code reviewed
- [ ] Deployed to staging
