---
name: Add is_first_login migration and update seed script
status: open
created: 2026-04-09T05:28:31Z
updated: 2026-04-09T05:28:31Z
github: [Will be updated when synced to GitHub]
depends_on: []
parallel: false
conflicts_with: []
---

# Task: Add is_first_login migration and update seed script

## Description
Create a database migration to add the `is_first_login` boolean column to the `users` table. Update the seed script to set `is_first_login = true` for all seeded users and remove plaintext password logs from stdout.

## Acceptance Criteria
- [ ] Migration file `database/migrations/002_add_is_first_login.sql` adds `is_first_login BOOLEAN DEFAULT true` to `users`
- [ ] Migration runs successfully and schema is updated
- [ ] `database/seeds/seed.js` sets `is_first_login: true` for all inserted users
- [ ] Seed script no longer prints plaintext passwords to stdout
- [ ] Existing users in dev/test environments are migrated correctly

## Technical Details
- Migration path: `database/migrations/002_add_is_first_login.sql`
- Seed file: `database/seeds/seed.js`
- Remove or comment out the `console.log` lines that print `admin123` and player passwords
- Ensure backward compatibility: default `true` so existing rows get the flag

## Dependencies
- [ ] PostgreSQL database and `users` table must already exist
- [ ] Migration runner (`npm run migrate` or equivalent) must be functional

## Effort Estimate
- Size: S
- Hours: 2
- Parallel: false

## Definition of Done
- [ ] Code implemented
- [ ] Tests written and passing
- [ ] Documentation updated
- [ ] Code reviewed
- [ ] Deployed to staging
