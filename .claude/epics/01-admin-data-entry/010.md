---
name: Frontend-Backend Integration
status: completed
created: 2026-04-08T03:50:18Z
updated: 2026-04-08T03:50:18Z
github: https://github.com/vinnie-luckfocus/lp2601-joyful-web/issues/11
depends_on: []
parallel: false
conflicts_with: []
---

# Task: Frontend-Backend Integration

## Description
连接前端和后端，实现端到端数据流，验证种子数据在前端正确显示。

## Acceptance Criteria
- [ ] 配置 Axios 实例和拦截器
- [ ] 实现登录功能（调用后端 API）
- [ ] 存储 JWT token（localStorage）
- [ ] 创建 Auth Store（Zustand）
- [ ] 保护 Admin 路由（需登录）
- [ ] 获取并显示种子数据
- [ ] 验证端到端数据流
- [ ] 错误处理完善

## Technical Details

### Axios 配置
```typescript
// utils/axios.ts
import axios from 'axios';

const api = axios.create({
  baseURL: import.meta.env.VITE_API_URL || 'http://localhost:3001/api',
});

api.interceptors.request.use((config) => {
  const token = localStorage.getItem('token');
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});

api.interceptors.response.use(
  (response) => response,
  (error) => {
    if (error.response?.status === 401) {
      localStorage.removeItem('token');
      window.location.href = '/login';
    }
    return Promise.reject(error);
  }
);
```

### Auth Store
```typescript
// stores/auth.ts
import { create } from 'zustand';

interface AuthState {
  user: User | null;
  isAuthenticated: boolean;
  login: (username: string, password: string) => Promise<void>;
  logout: () => void;
}

export const useAuthStore = create<AuthState>((set) => ({
  user: null,
  isAuthenticated: false,
  login: async (username, password) => {
    const { data } = await api.post('/auth/login', { username, password });
    localStorage.setItem('token', data.token);
    set({ user: data.user, isAuthenticated: true });
  },
  logout: () => {
    localStorage.removeItem('token');
    set({ user: null, isAuthenticated: false });
  },
}));
```

### 验证清单
- [ ] 登录表单提交成功
- [ ] JWT token 存储在 localStorage
- [ ] 后续请求自动携带 token
- [ ] 获取球队列表并显示
- [ ] 获取队员列表并显示
- [ ] 获取比赛列表并显示
- [ ] 数据与种子数据一致

## Dependencies
- [x] 001-005 - Backend tasks
- [x] 006-009 - Frontend tasks

## Effort Estimate
- Size: S
- Hours: 6h
- Parallel: false

## Definition of Done
- [ ] 登录功能完整可用
- [ ] 受保护路由需要认证
- [ ] Token 过期自动跳转登录
- [ ] 种子数据在前端正确显示
- [ ] 2 球队、18+ 队员、4 周赛程数据完整
- [ ] 无控制台错误
- [ ] 端到端测试通过
