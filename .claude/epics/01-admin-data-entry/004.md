---
name: JWT Authentication Middleware
status: open
created: 2026-04-08T03:50:18Z
updated: 2026-04-08T03:50:18Z
github: https://github.com/vinnie-luckfocus/lp2601-joyful-web/issues/5
depends_on: [001, 002]
parallel: true
conflicts_with: []
---

# Task: JWT Authentication Middleware

## Description
实现 JWT 认证系统，包括 token 生成、验证中间件和登录/登出接口。

## Acceptance Criteria
- [ ] 实现 JWT token 生成（7天有效期）
- [ ] 实现 JWT token 验证中间件
- [ ] 创建 POST /api/auth/login 接口
- [ ] 创建 POST /api/auth/logout 接口
- [ ] 创建 GET /api/auth/me 获取当前用户信息
- [ ] 密码使用 bcrypt 加密存储
- [ ] 错误处理完善（401, 403）
- [ ] 配置 JWT secret 环境变量

## Technical Details

### JWT 实现
```typescript
// middleware/auth.ts
export const generateToken = (userId: string): string => {
  return jwt.sign({ userId }, process.env.JWT_SECRET!, { expiresIn: '7d' });
};

export const verifyToken = (req: Request, res: Response, next: NextFunction) => {
  const token = req.headers.authorization?.split(' ')[1];
  if (!token) return res.status(401).json({ error: 'No token' });

  try {
    const decoded = jwt.verify(token, process.env.JWT_SECRET!);
    req.user = decoded;
    next();
  } catch {
    res.status(401).json({ error: 'Invalid token' });
  }
};
```

### API 端点
```
POST /api/auth/login
Request: { username: string, password: string }
Response: { token: string, user: { id, name, role } }
Error: 401 用户名或密码错误

POST /api/auth/logout
Headers: Authorization: Bearer {token}
Response: { success: true }

GET /api/auth/me
Headers: Authorization: Bearer {token}
Response: { id, name, team_id, jersey_number, position, role }
```

### 实现步骤
1. 安装 jsonwebtoken 和 bcrypt
2. 创建 auth 中间件
3. 实现登录逻辑
4. 实现 token 验证
5. 创建 auth 路由
6. 测试登录流程

## Dependencies
- [x] 001 - Database Schema Design
- [x] 002 - Database Seed Data
- [ ] 003 - Express Project Initialization

## Effort Estimate
- Size: M
- Hours: 8h
- Parallel: true

## Definition of Done
- [ ] 登录接口返回有效 JWT
- [ ] Token 验证中间件工作正常
- [ ] 受保护路由需要认证
- [ ] Token 过期后返回 401
- [ ] 密码验证使用 bcrypt
