---
name: Navigation Menu Component
status: open
created: 2026-04-08T03:50:18Z
updated: 2026-04-08T03:50:18Z
github: https://github.com/vinnie-luckfocus/lp2601-joyful-web/issues/10
depends_on: [006, 007, 008]
parallel: true
conflicts_with: []
---

# Task: Navigation Menu Component

## Description
创建配置化的导航菜单组件，支持图标+文字，集成路由跳转。

## Acceptance Criteria
- [ ] 创建 Navigation 组件
- [ ] 支持菜单配置（图标、文字、路径）
- [ ] 当前路由高亮显示
- [ ] 使用 Lucide React 图标
- [ ] 菜单项与路由联动
- [ ] 支持子菜单（可选）
- [ ] 响应式支持（移动端抽屉模式）

## Technical Details

### 菜单配置
```typescript
// config/menu.ts
export interface MenuItem {
  icon: LucideIcon;
  label: string;
  path: string;
  children?: MenuItem[];
}

export const adminMenu: MenuItem[] = [
  { icon: Home, label: '首页', path: '/admin' },
  { icon: Users, label: '球队管理', path: '/admin/teams' },
  { icon: UserCircle, label: '队员管理', path: '/admin/players' },
  { icon: Calendar, label: '赛程管理', path: '/admin/games' },
  { icon: BarChart3, label: '数据录入', path: '/admin/stats' },
  { icon: Video, label: '视频管理', path: '/admin/videos' },
];
```

### 组件实现
```typescript
// components/Navigation/index.tsx
interface NavigationProps {
  menuItems: MenuItem[];
  collapsed?: boolean;
}

export const Navigation: React.FC<NavigationProps> = ({ menuItems }) => {
  const location = useLocation();

  return (
    <nav className="flex flex-col gap-1">
      {menuItems.map((item) => (
        <NavLink
          key={item.path}
          to={item.path}
          className={({ isActive }) =>
            cn(
              'flex items-center gap-3 px-4 py-3 rounded-lg transition-colors',
              isActive
                ? 'bg-white/10 border-l-4 border-mlb-red text-white'
                : 'text-gray-300 hover:bg-white/5 hover:text-white'
            )
          }
        >
          <item.icon className="w-5 h-5" />
          <span>{item.label}</span>
        </NavLink>
      ))}
    </nav>
  );
};
```

### 实现步骤
1. 定义菜单配置类型
2. 安装 Lucide React
3. 创建 Navigation 组件
4. 集成 React Router NavLink
5. 添加激活状态样式
6. 集成到 Sidebar
7. 测试路由跳转

## Dependencies
- [x] 006 - React + Vite Project Initialization
- [x] 007 - Tailwind MLB Theme Configuration
- [x] 008 - Admin Layout Framework

## Effort Estimate
- Size: XS
- Hours: 4h
- Parallel: true

## Definition of Done
- [ ] 菜单配置可正常渲染
- [ ] 点击菜单项正确跳转路由
- [ ] 当前路由高亮显示（红色左边框）
- [ ] 图标和文字对齐
- [ ] 悬停效果工作正常
- [ ] 代码可复用，配置化
