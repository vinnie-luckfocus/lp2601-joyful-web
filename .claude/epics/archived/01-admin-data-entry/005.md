---
name: Admin Authorization Middleware
status: completed
created: 2026-04-08T03:50:18Z
updated: 2026-04-08T03:50:18Z
github: https://github.com/vinnie-luckfocus/lp2601-joyful-web/issues/6
depends_on: []
parallel: true
conflicts_with: []
---

# Task: Admin Authorization Middleware

## Description
实现管理员权限控制中间件，保护管理员专用 API 路由。

## Acceptance Criteria
- [ ] 创建管理员权限检查中间件
- [ ] 保护 /api/admin/* 所有路由
- [ ] 非管理员访问返回 403 Forbidden
- [ ] 中间件在 JWT 验证之后执行
- [ ] 错误信息清晰明确
- [ ] 支持开发环境绕过（可选）

## Technical Details

### 权限中间件
```typescript
// middleware/admin.ts
export const requireAdmin = (req: Request, res: Response, next: NextFunction) => {
  if (!req.user) {
    return res.status(401).json({ error: 'Authentication required' });
  }

  if (req.user.role !== 'admin') {
    return res.status(403).json({ error: 'Admin access required' });
  }

  next();
};
```

### 路由保护
```typescript
// routes/admin/index.ts
import { verifyToken } from '../../middleware/auth';
import { requireAdmin } from '../../middleware/admin';

const router = Router();

// 所有 admin 路由都需要认证 + 管理员权限
router.use(verifyToken, requireAdmin);

router.post('/teams', ...);
router.post('/users', ...);
router.post('/games', ...);
```

### 实现步骤
1. 创建 admin 中间件
2. 查询用户角色验证权限
3. 应用到 admin 路由
4. 测试权限控制
5. 完善错误处理

## Dependencies
- [x] 001 - Database Schema Design
- [x] 002 - Database Seed Data
- [x] 003 - Express Project Initialization
- [x] 004 - JWT Authentication Middleware

## Effort Estimate
- Size: S
- Hours: 6h
- Parallel: true

## Definition of Done
- [ ] 管理员可访问所有 /api/admin/* 路由
- [ ] 普通用户访问返回 403
- [ ] 未认证用户返回 401
- [ ] 中间件链正常工作
- [ ] 测试用例通过
