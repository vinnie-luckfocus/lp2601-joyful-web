---
name: batting_records_schema
status: open
created: 2026-04-09T22:17:17Z
updated: 2026-04-09T22:21:07Z
github: https://github.com/vinnie-luckfocus/lp2601-joyful-web/issues/51
depends_on: []
parallel: true
conflicts_with: []
---

# Task 001: Database Schema and Migrations for batting_records

## Description

Create the `batting_records` table migration with all required fields and add seed data for local development and testing.

## Acceptance Criteria

- [ ] Migration file adds `batting_records` with: `id`, `game_id`, `user_id`, `batting_order`, `position`, `pa_result`, `inning`, `created_at`, `updated_at`
- [ ] Foreign keys reference `games(id)` and `users(id)` with `ON DELETE CASCADE`
- [ ] Seed script inserts realistic batting records across multiple games and players
- [ ] Migration runs successfully in local dev and test environments

## Technical Details

- Use existing migration tooling (Knex or similar)
- `pa_result` enum values: `1B`, `2B`, `3B`, `HR`, `BB`, `K`, `OUT`, `SAC`, `HBP`, `RBI`
- Add indexes on `(game_id, user_id)` and `(user_id)` for query performance

## Dependencies

- `games` and `users` tables must exist

## Effort Estimate

3 hours

## Definition of Done

- Migration file reviewed and committed
- Seed data verified with manual query
- No rollbacks needed
